import React from 'react';
import { StyleSheet } from 'react-native';
import {
  Container,
  Button,
  Text,
  Thumbnail,
} from 'native-base';
import * as firebase from 'firebase';

export default class AddSpeeding extends React.Component {
  static navigationOptions = {
    header: null,
  };
}